[project]
name = "mcp-remote-deploy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Riyaj Atar", email = "55338028+RiyazAtar@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=2.2.0",
]

[project.scripts]
mcp-remote-deploy = "mcp_remote_deploy:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
